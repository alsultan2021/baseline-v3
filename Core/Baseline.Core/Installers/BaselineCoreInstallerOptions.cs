namespace Baseline.Core;

/// <summary>
/// Configuration options for the Baseline Core module installer.
/// </summary>
public record BaselineCoreInstallerOptions(
    bool AddMemberFields = true
);
